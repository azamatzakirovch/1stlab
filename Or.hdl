 /**
 * Or gate:
 * out = 1 if (a == 1 or b == 1)
 *       0 otherwise
 */

 CHIP Or {
    IN a, b;
    OUT out;

    PARTS:
    // Use an OR gate to combine the two inputs
    OrGate(a=a, b=b, out=out);
}

